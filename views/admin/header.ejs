<!DOCTYPE html>
<html lang="es" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChatBot SaaS - Admin</title>
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/logo.png">
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/pages.css"> <!-- Reuse main styles -->
    <link rel="stylesheet" href="/admin.css"> <!-- Admin specific styles -->
    <% if (typeof css !== 'undefined') { %>
        <link rel="stylesheet" href="<%= css %>">
    <% } %>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/admin">
                <img src="/logo.png" alt="Logo ChatBot SaaS" width="30" height="30" class="d-inline-block align-text-top me-2">
                Admin ChatBot SaaS
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="adminNavbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/instances">Instancias</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/users">Usuarios</a>
                    </li>
                    <!-- Theme Toggle -->
                    <li class="nav-item ms-2">
                        <button id="theme-toggle" class="btn btn-outline-light" type="button" title="Cambiar tema">
                            <span id="theme-icon">🌙</span>
                        </button>
                    </li>
                </ul>
            </div>
        </div>
    </nav>